<%- include('partials/header', { title: t('landing.title') }) %>
<section class="hero">
  <p class="hero__eyebrow"><%= t('landing.eyebrow') %></p>
  <h1 class="hero__title">Projeto <span class="grad">Profissional</span></h1>
  <p class="hero__sub"><%= t('landing.intro') %></p>
  <div class="hero__actions">
    <a class="btn-pp btn-pp--primary" href="/login"><%= t('landing.cta') %></a>
    <a class="btn-pp btn-pp--ghost" href="/demo/start"><%= t('landing.demoCta') %></a>
  </div>
  <div class="hero__badges">
    <span class="chip"><%= t('landing.f1title') %></span>
    <span class="chip"><%= t('landing.f2title') %></span>
    <span class="chip"><%= t('landing.f3title') %></span>
  </div>
</section>
<%- include('partials/footer') %>
